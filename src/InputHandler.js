/* 
  canvas.canvas.addEventListener("keydown", (event) ||| "keyup"

  if (event.key == "q") {
    Q = true;
  }

  mouse 
  canvas.addEventListener("mouseup", (event)  || mousedown || mousemove
  event.button === 0 -> left ||| 2 -> right

  canvas.canvas.addEventListener("mousemove", (event) => {
  mouse.x = event.clientX - canvasRect.left;
  mouse.y = event.clientY - canvasRect.top;
});

*/
